interface:
  display_name: "Roll Workspace Create"
  short_description: "Preview and create a complete Roll Workspace"
  default_prompt: "Use $roll-ws-create to preview a Roll Workspace from a versioned create config, then apply only after exact owner authorization."
